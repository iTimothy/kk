const state = {
    count: 0,
    todos: [
        {
            text:'first',
            done:false
        },
        {
            text:'second',
            done:true
        }
    ]
};

export default state;
